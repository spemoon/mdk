define(function(require, exports, module) {
    require('{sample}');
    var $ = require('jquery');
    // var xxx = require('{{pathToJs}}');
    var modName = require('{mod}');

    $(function() {
        /**--------------------------------------------
         * 实例1：
         * --------------------------------------------*/
        (function() {

        })();
    });
});
