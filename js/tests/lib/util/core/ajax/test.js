define(function(require, exports, module) {
    var qunit = require('../../../../../lib/external/qunit/sea_qunit.js');
    var ajax = require('../../../../../lib/util/core/ajax.js');

    qunit.module("util.core.ajax");

    qunit.test('base(params)', function() {

    });

    qunit.test('single(name)', function() {

    });

    qunit.test('pool(name, max, priority)', function() {

    });
});